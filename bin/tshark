#!/bin/bash

# Usage:
#   ./bin/tshark [interface] [channel]

ifconfig "$1" down
iwconfig "$1" mode managed
ifconfig "$1" up
iwconfig "$1" channel "$2"

tshark -i "$1" -I -f "type mgt subtype deauth" &
pid=$!
while read line ; do
  :
done
kill -KILL $pid
